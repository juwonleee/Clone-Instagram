package front.home;

import javax.swing.*;
import java.awt.*;

public class Line extends JPanel {

    public Line() {
        setPreferredSize(new Dimension(470, 1));
        setBackground(Color.gray);
    }
}
